# Core FastAPI dependencies
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
mangum>=0.17.0
pydantic>=2.12.4
pydantic-settings>=2.12.0
python-multipart>=0.0.20

# Database
sqlalchemy>=2.0.0
duckdb>=1.0.0

# Utilities
python-dotenv>=1.2.1
pyyaml>=6.0.3
requests>=2.31.0
openpyxl>=3.1.5
pandas>=2.3.3

# LLM / DSPy
dspy-ai>=3.0.4

# Note: The following heavy ML packages from the local dev environment
# are intentionally NOT installed on Vercel due to size limits:
# - torch
# - sentence-transformers
# - faiss-cpu
# - mlflow
# If you need full ML functionality in production, consider running the
# backend on a platform with larger limits (Railway, Render, etc.)


